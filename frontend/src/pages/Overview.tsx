/**
 * Development placeholder confirming the frontend foundation itself:
 * shell, routing, and styling all work. Product dimensions (starting
 * with Inflation) are added as their own increments -- see
 * docs/ENGINEERING_JOURNAL.md's Increment #16A entry.
 */
export function OverviewPage() {
  return (
    <div className="max-w-2xl">
      <h1 className="text-2xl font-semibold tracking-tight text-neutral-900">Economic Intelligence</h1>
      <p className="mt-3 text-neutral-600">Frontend foundation ready.</p>
      <p className="mt-2 text-neutral-600">
        Deterministic backend intelligence will appear here as each product dimension is built, starting with
        Inflation.
      </p>
    </div>
  );
}
